^!f::
	HotKey, ^f, off
	sendInput, ^{f}
	HotKey, ^f, on
Return

^f::
	send, {right}
RETURN

^b::
	send, {left}
RETURN

!f::
	send, ^{right}
RETURN
!b::
	send, ^{left}
RETURN
^n::
	send, {down}
RETURN
^p::
	send, {up}
RETURN
^a::
	send, {home}
RETURN

^e::
	send, {end}
RETURN


